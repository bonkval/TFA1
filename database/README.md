# Database export note

This module does not use a database. Customer and user accounts are intentionally represented by static PHP arrays in the controllers as required by the activity. A SQL schema/export belongs to the next module when the data layer is introduced.
